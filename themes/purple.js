const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.purple[600],
    primaryLight: colors.purple[200],
    primaryDark: colors.purple[800],
}

module.exports = {
    ...pallete,
};
