const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.orange[600],
    primaryDark: colors.orange[800],
    primaryLight: colors.orange[200],
}

module.exports = {
    ...pallete,
};
