const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.pink[600],
    primaryDark: colors.pink[800],
    primaryLight: colors.pink[200],
}

module.exports = {
    ...pallete,
};
