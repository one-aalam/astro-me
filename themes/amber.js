const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.amber[600],
    primaryDark: colors.amber[800],
    primaryLight: colors.amber[200],
}

module.exports = {
    ...pallete,
};
