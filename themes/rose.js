const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.rose[600],
    primaryLight: colors.rose[200],
    primaryDark: colors.rose[800],
}

module.exports = {
    ...pallete,
};
