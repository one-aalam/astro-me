const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.indigo[600],
    primaryDark: colors.indigo[800],
    primaryLight: colors.indigo[200],
}

module.exports = {
    ...pallete,
};
