const colors = require('tailwindcss/colors')

const pallete = {
    primary: colors.lime[600],
    primaryDark: colors.lime[800],
    primaryLight: colors.lime[200],
}

module.exports = {
    ...pallete,
};
